
# Local Claude Code tooling (preview harness, launch config) — not part of the site
.claude/

# Local testing checklist (Mike's working doc, not kit content)
testing/
HANDOFF-*.md

# Cross-post drafts and marketing notes (working docs, not site content)
marketing/
